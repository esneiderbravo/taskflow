FRONTEND_PORT=3000
BACKEND_PORT=8000
POSTGRES_PORT=5432
NEXT_PUBLIC_API_URL=http://localhost:8000

# Inside Docker Compose the backend uses hostname `postgres` (see docker-compose.yml).
# DATABASE_URL=postgresql://taskflow:taskflow@postgres:5432/taskflow
